
//location.reload();





    contadorCH=contadorCH+1;
    // alert(contadorCH);
    // alert('holaaaa');
    if(contadorCH ==2){
        location.reload();
    }


setTimeout(function(){
    var t = document.getElementById("nombre");
    if(t.value.length < 3){
        window.location.replace("/sadministrator/login/request/on/");
	   
    }

    const form_chat = document.getElementById("contactForm");
    function contactForm(e) {
        // count=count+1;
        // console.log('numero de clicks es = a '+count);
        e.preventDefault();
        var t = document.getElementById("nombre"),
            n = document.getElementById("email"),
            a = document.getElementById("telefono"),
            o = document.getElementById("mensaje"),
            c = document.getElementById("programa").value;
        var s = new Date;
        var m = s.getFullYear() + "-" + (s.getMonth() + 1) + "-" + s.getDate(),
            r = s.getHours() + ":" + s.getMinutes()  + ":" + s.getSeconds();

        if(o.value.trim().length > 0){
            saveContactForm({
                name: t.value,
                email: n.value,
                telefono: a.value,
                mensaje: o.value,
                origen: "",
                fecha: m,
                hora: r
            }, c), form_chat.reset()
        }

    }
	
    form_chat && form_chat.addEventListener("submit", contactForm);

    function saveContactForm(e, t) {
        urlbase=location.href;
        urlbaseSanity= urlbase.split(".").join("_");
        urlbaseSanity= urlbaseSanity.split("/").join("_");
        chat=document.getElementById("chat").value;
        room = "yachai_Chat_/"+chat ;

        firebase.database().ref(room).push(e).then(function() {
        <!-- document.getElementById("contactForm").innerHTML = "<span class='bg-success py-2 px-5'><small class='font-weight-bold text-white'>En breve nos pondrémos en contacto con ud.</small></span>" -->
        }).catch(function() {
            alert("mensaje No enviado")
        })
    }





    $(document).ready(function(){
        //c = document.getElementById("programa").value;

        urlbase=location.href;
        urlbaseSanity= urlbase.split(".").join("_");
        urlbaseSanity= urlbaseSanity.split("/").join("_");
        chat=document.getElementById("chat").value;
        room = "yachai_Chat_/"+chat ;

        $('#loadingChat').css({ 'width':'100%'})

        setTimeout(function(){
            //$("#chatPanel div").remove();

            setTimeout(function(){
                $("#progressChat").fadeOut('slow');
            }, 1500);


            firebase.database().ref(room).limitToLast(100).orderByKey().on('child_added', function(data){
				
                var email = data.val().email;
                if(email == 'brianvalverde1598@gmail.com' || email == 'defs.centurion@gmail.com' || email == "jperez@ecopetroleum.com.pe"){
                    var estileAdmin=';background: #ffeb3b40;';
                    var moderador = '<span style="font-size: 8px;font-weight: 700;color: #0c0c0c;"> MODERADOR</span> <img style="width: 14px;margin-top: -4px;" src="/simages/icons/corona.png">';
                }else{
                    var estileAdmin=';background: #c2e3ff;';
                    var moderador = '';
                }

                $('#chatPanel').append(`<div style="list-style: none;padding: 5px 10px 5px 10px;margin: 10px 0px 10px 0px;width: 98%;${estileAdmin}">
                <div> ${data.val().mensaje}</div>
                <div><span><b>${data.val().name}</b> </span><span style="font-size:0.85em;"> |   ${data.val().fecha} ${data.val().hora}</span> ${moderador}</div>
               </div>`);
                $("#chatPanel").animate({ scrollTop: $("#chatPanel").prop("scrollHeight")}, 30);
                //console.log(data.val());
            });

        }, 200);

    });
    $('#loadingChat').css({ 'width':'75%'});

}, 1000);



setTimeout(function(){
    $('#loadingChat').css({ 'width':'55%'});
}, 500);

